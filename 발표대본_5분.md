# S.O.S – Solve to Stop 발표 대본 (5분)

구성: 데모 시연 → 기술 스택 설명 → 마무리. 학교 프로젝트 발표(교수/동기 대상) 기준으로,
기획 의도와 배운 점이 드러나도록 짰다. 시간은 여유 있게 잡았으니 실제 리허설하면서
±10초는 조정해도 됨.

---

## 0:00 – 0:25 오프닝 (25초)

> "혹시 아침에 알람 끄고 다시 잠든 경험 있으신가요? 저희 팀은 여기서 시작했습니다.
> 그냥 끄는 알람이 아니라, **공부한 내용으로 만든 퀴즈를 풀어야 꺼지는 알람** —
> 'S.O.S, Solve to Stop'을 만들었습니다. 먼저 실제 작동하는 모습을 보여드릴게요."

화면: 앱 아이콘/타이틀 화면 or 홈 화면에서 시작.

---

## 0:25 – 2:45 데모 (2분 20초)

### ① PDF 업로드 → AI 요약 (20초)
- 학습자료 탭 → 폴더 생성 → PDF 업로드
- 대본: "학습할 PDF를 올리면 서버가 백그라운드에서 파싱하고, Gemini가 핵심 내용을
  요약해줍니다. 지금 업로드 중이고, 이미 요약이 끝난 자료로 넘어가서 보여드릴게요."
- 화면: 요약 완료된 자료 화면(AiSummaryScreen)의 "요약" 카드 보여주기.

### ② AI 학습송 생성 (20초)
- 같은 화면에서 "학습자료로 노래 만들기" 버튼
- 대본: "이 요약을 바탕으로 AI가 직접 가사를 쓰고, Google의 음악 생성 모델로 실제
  노래까지 만들어줍니다. 이 노래를 알람음으로 쓸 수 있어요."
- 화면: 노래 생성 완료 상태(가사 + 미리듣기 버튼) 짧게 재생.

### ③ 알람 설정 (20초)
- 알람 탭 → 알람 추가 → 시간 설정, 방금 만든 세트 연결, 알람음으로 방금 만든 노래 선택
- 대본: "알람을 만들 때 이 학습 세트를 연결하고, 알람음도 방금 만든 노래로 지정합니다.
  요일 반복, 시간은 시계나 키패드로 자유롭게 설정할 수 있어요."

### ④ 알람 울림 → 퀴즈 → 해제 (40초) — 가장 중요한 파트
- 홈 화면의 "가상 문제풀이" 또는 실제 알람으로 트리거
- 대본: "알람이 울리면 바로 꺼지지 않고, 그 학습자료 기반 문제가 나옵니다. 서버가
  미리 생성해둔 문제 중 몇 개를 내려주고, 한 문제씩 풀면 정답 여부와 해설을 바로
  알려줍니다. 3문제 중 2개 이상 맞히면 — 지금처럼 — 알람이 꺼집니다."
- 화면: 문제 풀이 2~3개 실제로 클릭 → 정답/오답 피드백 → 마지막 문제 후 알람 꺼짐.

### ⑤ (선택) 오답 시 동작 미션 (15초)
- 시간 여유 있으면: "만약 과반수를 못 맞히면, 대신 몸을 움직여서 확실히 깨우는
  동작 미션 화면으로 넘어갑니다." — 화면 짧게 보여주고 스킵.

### ⑥ 리포트 / 약점 분석 (15초)
- 대본: "풀고 나면 오늘의 리포트와, 이 자료에서 자주 틀리는 주제를 분석한 약점
  분석·오답노트도 함께 쌓입니다."
- 화면: TodayReportScreen 또는 AiSummaryScreen의 약점 분석 카드.

---

## 2:45 – 4:40 기술 스택 설명 (1분 55초)

### ① 전체 아키텍처 (25초)
- 대본: "구조를 간단히 설명드리면, Flutter 앱이 REST API로 FastAPI 백엔드와 통신하고,
  백엔드는 PostgreSQL에 데이터를 저장하고, PDF 파일은 AWS S3에, AI 처리는 Google의
  Gemini와 Lyria 모델을 씁니다."
- (화면에 아키텍처 다이어그램 있으면 이 타이밍에 띄우기 — 없으면 말로만 설명해도 무방)

### ② 프론트엔드 (20초)
- 대본: "프론트는 Flutter/Dart로 만들었고, 실제 기기 알람은 `alarm` 패키지로 백그라운드/
  종료 상태에서도 울리게 구현했습니다. 화면 간에 상태를 공유하는 자체 데이터 계층을
  만들어서, 알람·학습자료 목록이 항상 서버와 동기화되도록 했습니다."

### ③ 백엔드 (25초)
- 대본: "백엔드는 FastAPI + SQLAlchemy + Alembic 마이그레이션으로 PostgreSQL을 관리합니다.
  PDF는 클라이언트가 presigned URL로 S3에 직접 업로드하고, 서버가 백그라운드에서
  pdfplumber로 텍스트를 추출해 파싱합니다."

### ④ AI 파이프라인 (25초)
- 대본: "요약과 노래 가사는 Gemini 2.5 Flash-Lite가 생성하고, 실제 노래(음원)는
  Vertex AI의 Lyria 3 모델로 만듭니다. 퀴즈 문제는 알람이 울릴 때마다 즉석으로
  만드는 게 아니라, 학습 세트별로 미리 버퍼에 쌓아두는 방식이라 알람이 울렸을 때
  바로 문제를 받을 수 있습니다."

### ⑤ 인증/보안 (15초)
- 대본: "로그인은 카카오 OAuth와 자체 JWT 토큰 기반으로 처리하고, 토큰이 만료되면
  자동으로 재발급 후 요청을 재시도하도록 만들었습니다."

### ⑥ (선택) 겪었던 기술적 문제 (25초) — 시간 남으면
- 대본: "개발하면서 실제로 겪었던 문제도 하나 말씀드리면, 처음엔 퀴즈를 다 맞혀야만
  알람이 꺼지도록 서버가 설계돼 있어서, '3문제 중 2개 이상'이라는 저희 기획 의도와
  안 맞았습니다. 그래서 클라이언트가 직접 통과 기준을 판정하고, 서버의 대체 해제
  경로를 활용해서 해결했습니다." — (실제로 겪은 디버깅 경험이라 신뢰도/깊이를 보여주기 좋음)

---

## 4:40 – 5:00 마무리 (20초)

> "정리하면, S.O.S는 '공부 + 기상'이라는 두 가지 습관을 하나의 알람으로 엮은 서비스입니다.
> 지금은 개인 학습용으로 쓰고 있지만, 앞으로 친구와 문제 대결하는 기능이나, 학습
> 통계를 더 정교하게 보여주는 방향으로 발전시킬 계획입니다. 감사합니다."

---

## 발표 팁
- 데모 파트가 가장 중요함 — ④(퀴즈 풀이→알람 꺼짐)는 절대 시간 넘겨서 서두르지 말 것.
  대신 ⑤(동작 미션)나 기술 스택의 ⑥(겪었던 문제)은 시간 부족하면 통째로 스킵 가능하도록
  설계해뒀음.
- 데모는 미리 한 번 리허설해서 "AI 분석 중"처럼 기다려야 하는 구간(PDF 파싱, 노래 생성)은
  발표 전에 미리 만들어둔 자료로 넘어가는 게 안전함 (실시간 생성은 20~30초 이상 걸릴 수 있음).
- 질의응답에서 나올 만한 질문: "왜 알람이 꺼지는 기준이 전부 정답이 아니라 과반수냐" →
  기획 의도(너무 엄격하면 오히려 사용자가 앱을 지워버릴 수 있어서) 답변 준비.
